#include "paintFrm.h"
extern short isbutton;
static short b=0;
void paintFrm::OnMotion(wxMouseEvent& event)
{
  /*if (event.Dragging())
    {
    wxClientDC dc(this);
    wxPen pen(*wxBLUE, 1); // red pen of width 1
    dc.SetPen(pen);
    dc.DrawPoint(2,2);
    dc.DrawPoint(3,3);
    dc.DrawPoint(4,4);
    dc.SetPen(pen);
    dc.DrawPoint(event.GetPosition());
    dc.SetPen(wxNullPen);
    }*/
}







